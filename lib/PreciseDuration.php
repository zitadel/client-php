<?php

/**
 * Zitadel SDK
 * The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * Generated by: https://openapi-generator.tech
 */

declare(strict_types=1);

namespace Zitadel\Client;

/**
 * A \DateInterval that preserves nanosecond fractional-second precision.
 *
 * google.protobuf.Duration permits up to nine fractional digits (nanoseconds),
 * but PHP's native \DateInterval::$f only stores microsecond precision: any
 * value below 1e-6 assigned to ->f on a stock \DateInterval is silently
 * truncated to 0.0, so "3600.000000001s" could never round-trip. This subclass
 * declares its own plain properties and bypasses the lossy native initialiser
 * (the parent constructor is intentionally not invoked), so ->f behaves as an
 * ordinary float and keeps the full nanosecond value. Instances remain
 * `instanceof \DateInterval`, so they flow through every existing duration
 * code path (formatProtobufDuration, the DurationNormalizer) unchanged.
 *
 * @category Class
 * @package  Zitadel\Client
 *
 * @internal This class is part of the SDK's internal serialization plumbing
 *           and is not part of the public API. It may change or be removed
 *           in any release without notice.
 */
final class PreciseDuration extends \DateInterval
{
    /*
     * \DateInterval's properties are declared without native types, so these
     * overrides must stay untyped too (PHPStan property.extraNativeType is
     * non-ignorable); the docblocks carry the types for static analysis.
     */
    /** @var int */
    public $y = 0;
    /** @var int */
    public $m = 0;
    /** @var int */
    public $d = 0;
    /** @var int */
    public $h = 0;
    /** @var int */
    public $i = 0;
    /** @var int */
    public $s = 0;
    /** @var float */
    public $f = 0.0;
    /** @var int */
    public $invert = 0;
    /** @var int|false */
    public $days = false;

    /**
     * Deliberately bypasses \DateInterval::__construct(): the native
     * initialiser routes ->f through microsecond-precision storage, which is
     * exactly the truncation this class exists to avoid. The properties above
     * are already initialised to a zero duration.
     */
    public function __construct()
    {
    }
}
